// import actions from "./actions";

const logUp = () => {
  // return dispatch => {
  //   dispatch(actions.increaseCounter());
  // };
  // actions.increaseCounter();
};

export default {
  logUp
};
