// import actions from "./actions";

const signUp = () => {
  debugger
  return async dispatch => {
    //   dispatch(actions.increaseCounter());
    return true;
  };
};

export default {
  signUp
};
